$VLESS_DOMAIN {

	@grpc {
		protocol grpc
		path /v2/*
	}

	reverse_proxy @grpc 127.0.0.1:8080 {
		flush_interval -1
		transport http {
			versions h2c
		}
	}

	$CADDY_REVERSE

	header {
		-Server
	}

	handle_errors {
		header {
			-Server
		}
	}
}
